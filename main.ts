const calculateNoi = (income: number, expenses: number) => {
  return income - expenses;
};

const calculateValue = (noi: number, cap: number) => {
  return noi / cap;
};

console.log('hello world');
